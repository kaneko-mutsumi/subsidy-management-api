<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.subsidy_management_api.mapper.SubsidyApplicationMapper">

  <insert id="insert"
    parameterType="com.example.subsidy_management_api.domain.SubsidyApplication"
    useGeneratedKeys="true"
    keyProperty="id">
    INSERT INTO subsidy_applications (
    applicant_id,
    application_date,
    amount_requested,
    status
    ) VALUES (
    #{applicantId},
    #{applicationDate},
    #{amountRequested},
    #{status}
    )
  </insert>

  <select id="findList" resultType="com.example.subsidy_management_api.api.dto.SubsidyApplicationListItem">
    SELECT
    sa.id AS applicationId,
    a.id AS applicantId,
    a.full_name AS applicantFullName,
    sa.application_date AS applicationDate,
    sa.amount_requested AS amountRequested,
    sa.status AS status
    FROM subsidy_applications sa
    JOIN applicants a ON a.id = sa.applicant_id
    WHERE
    sa.deleted_at IS NULL
    AND a.deleted_at IS NULL
    <if test="status != null and status != ''">
      AND sa.status = #{status}
    </if>
    <if test="from != null">
      AND sa.application_date &gt;= #{from}
    </if>
    <if test="to != null">
      AND sa.application_date &lt;= #{to}
    </if>
    <if test="q != null and q != ''">
      AND a.full_name LIKE CONCAT('%', #{q}, '%')
    </if>
    ORDER BY sa.id DESC
  </select>

</mapper>
